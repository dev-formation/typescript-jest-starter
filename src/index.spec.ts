describe('Un ensemble de test', () => {
  test('Test exemple1', () => {
    const actual = 1;
    expect(actual).toBe(1);
  });

  test('Test exemple2', () => {
    const actual = 0;
    expect(actual).toBe(1);
  });
});
